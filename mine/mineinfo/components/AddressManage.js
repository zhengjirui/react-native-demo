import React,{Component} from 'react';
import {
    View,
    Text,
    Image,
    PixelRatio,
    StyleSheet,
    TouchableOpacity,
    Platform
} from 'react-native';

export default class AddressManage extends Component{
    static defaultProps ={
         renderText:null,
         renderDefaultFont:null,
         renderNumber:null,
         renderAddress:null,
    };

    render(){
        return(
            <View >


            </View>
        )
    }
}